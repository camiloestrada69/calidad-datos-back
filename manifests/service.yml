apiVersion: v1
kind: Service
metadata:
    name: camiloestradacalidaddatosback
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: camiloestradacalidaddatosback